import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Strict mode catches common bugs in development
  reactStrictMode: true,
  // Opt in to the App Router (default in Next 15)
  experimental: {},
}

export default nextConfig
